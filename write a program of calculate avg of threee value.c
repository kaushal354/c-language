#include <stdio.h>
main()
{
int a,b,c,avg;
printf("enter a value of a,b,c to calculate their avg\n");
scanf("%d",&a);
scanf("%d",&b);
scanf("%d",&c);
avg=(a+b+c)/3;
printf("the average of a,b,c is %d",avg);
getch();

}
